# Active System Specifications

This directory contains current, implementation-relevant descriptions of game systems.

Historical audits are stored under `docs/archive/`. They preserve rationale and checkpoints but are not the primary source of truth.

System specifications should describe current behavior, responsibilities, inputs/outputs, constraints, and cross-system interfaces. Concrete content lists may live in `data/` or dedicated content documents.
